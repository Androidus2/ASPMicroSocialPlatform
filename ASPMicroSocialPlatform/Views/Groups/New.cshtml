@model GroupCreateModel

<div class="card border-0 shadow-sm">
    <div class="card-header bg-white border-0 py-3">
        <div class="d-flex align-items-center">
            <i class="bi bi-people-fill fs-4 me-2"></i>
            <h2 class="mb-0">Create New Group</h2>
        </div>
    </div>

    <div class="card-body p-4">
        <form asp-action="New" method="post">
            <div class="mb-3">
                <label asp-for="Name" class="form-label">Group Name</label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Description" class="form-label">Description</label>
                <textarea asp-for="Description" class="form-control" rows="3"></textarea>
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label class="form-label">Members</label>
                <div class="input-group mb-2">
                    <input type="text" id="searchInput" class="form-control" placeholder="Search users...">
                    <span class="input-group-text">
                        <i class="bi bi-search"></i>
                    </span>
                </div>
                <div id="searchResults" class="border rounded p-2 mb-2" style="max-height: 200px; overflow-y: auto;"></div>
                <div id="selectedUsers">
                    <partial name="_SelectedUsersPartial" model="Model" />
                </div>
            </div>

            @* @foreach (var userId in Model.SelectedUserIds)
            {
                <input type="hidden" name="SelectedUserIds" value="@userId" />
            } *@

            <div class="d-flex justify-content-between">
                <a asp-action="Index" class="btn btn-outline-secondary">
                    <i class="bi bi-arrow-left me-2"></i>Back to Groups
                </a>
                <button type="submit" class="btn btn-primary">
                    <i class="bi bi-plus-lg me-2"></i>Create Group
                </button>
            </div>
        </form>
    </div>
</div>